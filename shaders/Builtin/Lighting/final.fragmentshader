#version 330 core

// Interpolated values from the vertex shaders
in vec2 UV;

uniform vec3 light;

// Output data
out vec4 color;

uniform sampler2D texSampler;

void main()
{
	color = vec4(texture( texSampler, UV ).rgba);
}