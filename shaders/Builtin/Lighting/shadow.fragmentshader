
#version 330 core

// Interpolated values from the vertex shaders
in vec2 UV;

// Output data
out vec4 color;

void main()
{
	// color = vec4(distance(light.xy, UV),0,0,light.w);
	color = vec4(1,1,1,0.5);
}